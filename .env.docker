# Docker Compose Environment Configuration
# Copy this file to .env and modify as needed

# Database Configuration
# Options: memory, sqlite, postgres
DB_TYPE=sqlite

# SQLite Configuration (when DB_TYPE=sqlite)
# Path will be /app/data/catalog.db inside container
DB_PATH=/app/data/catalog.db

# PostgreSQL Configuration (when DB_TYPE=postgres)
# Uncomment and configure if using PostgreSQL
# DB_NAME=catalog
# DB_USER=postgres
# DB_PASSWORD=password
# DB_HOST=postgres
# DB_PORT=5432

# Server Configuration
PORT=3000
NODE_ENV=development

# Storage Configuration
# Options: 
# - file:///tmp/warehouse (local filesystem)
# - s3://warehouse (MinIO/S3)
WAREHOUSE_PATH=s3://warehouse

# MinIO/S3 Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
S3_ENDPOINT=http://minio:9000

# For external S3 (AWS S3, not MinIO), comment out S3_ENDPOINT and use:
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key

# Spark Configuration
SPARK_DRIVER_MEMORY=2g
SPARK_EXECUTOR_MEMORY=2g